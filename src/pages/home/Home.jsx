import Button from "../../components/button/Button";

const Home = () => {
    return (
        <div>
            <Button onClick={() => {}} bgColor={"bg-black"} />
        </div>
    );
};

export default Home;
